# Revision 4 — Progress & Testing Guide

Status as of: 2026-09-24
Both builds pass: `npx tsc --noEmit` (server) and `npm run build` (root).

---

## 1. Progress track

### Done — code complete & builds verified

| # | Fix | Key file(s) |
|---|-----|-------------|
| 1 | LIS Excel export — fixed `getExcelLetter` helper (crash on export) | `server/src/controllers/lis.controller.ts` |
| 2 | Subject delete — FK protection, clear 409 message, deletes assignments first | `server/src/controllers/subjects.controller.ts` |
| 3 | Backup download — new `GET /backups/:id/download`, wired buttons, `utf8mb4` on backup + restore | `backups.controller.ts`, `backups.routes.ts`, `src/app/services/backups.ts`, `DatabaseBackup.tsx` |
| 4 | Room ↔ Schedule sync — rooms turn `Occupied`/`Available` automatically; `Maintenance`/`Inactive` preserved. **Fixed live**: schedule create/update/delete were 500-ing everywhere because migration `029` had never been applied (its SQL also dropped the wrong CHECK) — new **`031_room_status_occupiable.sql`** normalizes the `rooms.status` CHECK to allow `Occupied` (idempotent either way); `findConflicts` now detects overlaps on room **OR** teacher **OR** section (was AND — cross-room teacher double-bookings slipped through); `syncRoomStatus` no longer clobbers a manual `Maintenance`/`Inactive`. Frontend Schedule Modifier now picks a room from the rooms list and saves `room_id` (was free text → no link, no Occupied) | `server/migrations/031_room_status_occupiable.sql`, `schedules.controller.ts`, `ScheduleModifier.tsx` |
| 5 | School years list ordered numerically by year (desc) | `server/src/controllers/schoolYears.controller.ts` |
| 6 | User edit now saves `status` (was silently dropped) — role/status edits persist | `server/src/controllers/users.controller.ts` |
| 7 | Profile editability — Employee ID / Designation / Date Hired editable on all 4 profile pages | `auth.controller.ts`, `services/api.ts`, `ProfileSections.tsx`, 4× profile pages |
| 8 | SchoolSettings — Principal/Registrar required (with `*`), Active SY read‑only, enrollment dates normalized | `src/app/pages/admin/SchoolSettings.tsx` |
| 9 | TopBar — "Home" breadcrumb clickable → your role dashboard | `src/app/components/layout/TopBar.tsx` |
| 10 | AdminDashboard — stat card wording + green dot on Total Sections | `src/app/pages/admin/AdminDashboard.tsx` |
| 11 | PrincipalDashboard — pie shows `Name: value` + tooltip `X students` | `src/app/pages/principal/PrincipalDashboard.tsx` |
| 12 | AdminRooms — `Occupied` status displayed & filterable (not selectable in form) | `src/app/pages/admin/AdminRooms.tsx` |
| 13 | Backup resilience — `mysqldump` CLI can't auth to Railway MySQL (`caching_sha2_password`); falls back to a logical dump via the app's mysql2 connection; restore does the same. Manual AND scheduled backups now work | `server/src/utils/dbBackup.ts`, `backups.controller.ts`, `backupCron.ts` |
| 14 | Forgot Password → one-time reset **link** (was a 6-digit code) — tokenized `/reset-password` page, emailed link, token cleared on use/expiry. Live-tested end‑to‑end and reverted | `auth.controller.ts`, `mailer.ts`, `Login.tsx`, `ResetPassword.tsx`, `routes.tsx`, `services/api.ts` |
| 15 | Complete Terms of Service / Privacy Policy / Conditions of Use — full texts seeded into `school_settings` (editor shows them, login modals render them); covers collected / stored / used / security / retention / rights (RA 10173). Applied live + **migration `030_seed_legal_documents.sql`** for other environments | `server/migrations/030_seed_legal_documents.sql`, `Login.tsx` |
| 16 | SF1/SF5 edit bug — text columns (names, remarks, LRN, action…) were routed through the numeric sanitizer and reset to `0`; only the grade/numeric keys are sanitized now, and the sanitizer returns `''` instead of `0` on bad input | `sf1-register.tsx`, `sf5-report.tsx` |
| 17 | SF1/SF5 signatures auto-populate — "Prepared by"/Adviser = the signed-in account's name (`authApi.me()`), "Certified Correct"/School Head = `settings.principal_name`; all still editable | `sf1-register.tsx`, `sf5-report.tsx` |
| 18 | Database restore fixed — dump adds `DROP TABLE IF EXISTS` before every `CREATE TABLE` and relaxes/restores `sql_mode` (`SET @OLD_SQL_MODE…, SQL_MODE=''`), so empty ENUM rows no longer truncate and restore works on a populated DB. Verified live: fresh DB pass, existing-data pass, and a full `/backups/:id/restore` on the real DB | `server/src/utils/dbBackup.ts` |
| 19 | Backup dir stabilized — `server/backups` (resolved from `process.cwd()`, shared by controller + cron, `__dirname`-independent) | `server/src/utils/dbBackup.ts`, `backups.controller.ts`, `backupCron.ts` |
| 20 | Backup download — blob fetch with the `Authorization` header (replaces `window.open`+token query, so it can't be popup-blocked); simulated-progress interval is cleared on success AND failure | `src/app/services/backups.ts`, `DatabaseBackup.tsx` |
| 21 | Subject Management — "Assigned Teacher" column/header now clickable and expands inline to the assigned teacher(s), separate "Assign Teacher" action kept; subject's assignments purged from React state after delete | `src/app/pages/admin/SubjectManagement.tsx` |
| 22 | Activity Logs — desktop table scrolls inside its card (`max-h-[70vh]`, smooth + contained overscroll) | `ActivityLogs.tsx`, `src/styles/index.css` |
| 23 | Grade Summary (LIS) PDF — no more one-page squeeze. Rebuilt as several A4-landscape pages: fixed learner columns (LRN 70px, **Learner Name 200px**, Grade, Section) + Gen. Average/Promotion repeat on **every** page, at most 3 subjects per page, two-line header (subject name spanning Q1–Q4) with readable horizontal labels (no rotation), a practical 9px font (subject heads 8.5px), header repeated on every page (`thead` + chunk page-breaks), letterhead logos on each page. Rendered live via `/pdf/render` and layout-verified with headless Chrome (26 pages in dev data, each table 972px — fits A4 landscape) | `src/app/services/lisPdf.ts` |
| 24 | Grade Summary data scope — the pivot listed **every grade level's** subjects (78 in dev data: 13 subjects × 6 levels via the `(name, grade_level)` unique key), so pages 2+ repeated the same learners with empty columns. Now restricted to the grade level(s) of the filtered roster and, when grades exist, to the subjects that actually received them (fallback keeps graded subjects outside the level list). Dev data fell from **26 pages → 1**. Removed the "· Page X of Y" noise from the title | `server/src/controllers/lis.controller.ts`, `src/app/services/lisPdf.ts` |
| 25 | Grade Upload template download — was going through `window.open(…?token=…)`, which the popup blocker kills. Switched to a **same-tab anchor navigation** with the JWT as `?token=` (auth middleware accepts query tokens for download links). The server's `Content-Disposition: attachment` saves the file under its real name, so no popup blocking and no blob-URL lifecycle. Same approach applied to uploaded-document downloads and `DocumentManagement`, plus the old `saveBlob` helper was removed. Restored the button's `disabled={!hasSelection}` and handler guard — both had been left commented out, so an empty subject dropdown fired a 400 → "Template download failed". The Subject dropdown is now **not** filtered by `teacher_subject_assignments` — it lists every subject for the selected section's grade level, so the template (a blank roster) downloads regardless of whether the teacher is assigned that subject; the **upload/import step stays scoped** (`handleFileChosen` rejects unassigned subjects). Extended: `DatabaseBackup.tsx` no longer `.catch`es on the void download. Verified with a real-Chrome e2e: HTTP 200 with all params + token, download begins as `7-Rose_Q1_TLE_EPP_template.xlsx` (4659 bytes) | `src/app/services/documents.ts`, `backups.ts`, `UploadGrades.tsx`, `DatabaseBackup.tsx` |
| 26 | Profile save (Personal Information) — **every** Save returned 500 "Failed to update profile." for accounts with a hire date (Teacher, Registrar, Principal, Admin). Root cause: `getMe` serializes the mysql2 `DATE` as ISO-8601 UTC (`2019-06-08T16:00:00.000Z`), the edit form re-sends it untouched, and MySQL strict mode rejects `T`/`Z` in a `DATE` column → wrapped 500. Fix: `getMe`/`updateMe` now return `date_hired` as plain `YYYY-MM-DD` (idempotent round-trip) via a `dateOnly()` helper; `updateMe` accepts/validates dates (`YYYY-MM-DD`, leading-zero-normalised) and returns **400** for a bad date, **400** for out-of-range name/email/phone or a malformed email, and **409** for an email already taken (was a bare 500). The shared edit form renders Date Hired as a native `type="date"` picker so invalid formats can't be typed. Verified live for teacher/registrar/principal: getMe→save round-trips cleanly; the three edge cases return 400/409. Added client-side form validation in the shared edit card: required Full Name, email format, **PH phone format** (mobile `0917 123 4567` / `+63 917 123 4567`, landline `02-8123 4567`), DB-width limits mirrored as `maxLength` + inline red errors on blur/save (toast on first error, Save blocked). Only fields the user actually touched are validated (`dirty` tracking), so pre-existing legacy values never block a save. | `server/src/controllers/auth.controller.ts`, `src/app/components/ProfileSections.tsx` |
| 27 | School Config — Enrollment Period — the two `<input type="date">` fields **blanked out to the `mm/dd/yy` placeholder right after saving**. Root cause (same `DATE`-column class as #26): the school-years API returned `enrollment_start_date`/`enrollment_end_date` as mysql2 JS Dates → ISO-8601 UTC timestamps, and the save handler bound that raw string into the date inputs (unlike the load path, which normalized via `toDateInput`) — `<input type="date">` rejects anything that isn't `YYYY-MM-DD`, so the field rendered empty. Fix at the source: `schoolYears.controller.ts` now serializes every response through `serializeYear()`/`dateOnly()` → plain `YYYY-MM-DD` (list, current, create, update, set-current, archive). This also fixes Academic Year Management's date display. The SchoolSettings save handler additionally re-normalizes with `toDateInput` for defense-in-depth. | `server/src/controllers/schoolYears.controller.ts`, `src/app/pages/admin/SchoolSettings.tsx` |
| 28 | SF10 Permanent Record — **"Affix School Seal here" overlapped content**. The CERTIFICATION signature row used `grid grid-cols-3` + `1fr`, and measured (headless Chrome, real preview iframe) the seal column overflowed the certification box's right edge (box right 1094 vs seal col right 1096). Replaced the row with `flex items-end gap-6`: Date `w-40 shrink-0`, Principal `min-w-0 flex-1`, and the seal in an isolated fixed `w-36 shrink-0` slot (96px dashed circle placeholder). Re-measured at 1440 / 1366 / 1280 / 1024 → **zero overlaps** (seal clears the box by 38px and the principal block by 48px at every width) | `src/app/pages/registrar/sf10-report.tsx` |
| 29 | Academic Year Management — **school years listed in the wrong order**. The server already returns `DESC` (item 5), but the page's `sortYears()` re-sorted **ascending** with `localeCompare`, flipping the list. `sortYears()` now sorts numerically **latest-first** (start-year DESC, then end-year DESC), handling both `2026-2027` ranges and `2026` calendar labels. Verified live: DOM renders `2032, 2031, … 2025` (latest at top) vs the previous ascending `2025 … 2032` | `src/app/pages/admin/AcademicYearManagement.tsx` |
| 30 | Forgot Password on Railway — **reset email fails (`ETIMEDOUT` on `CONN`)**. Deployed env does have `GMAIL_USER`/`GMAIL_APP_PASSWORD` set (the transport is built), but the container cannot open a TCP connection to `smtp.gmail.com:465` — a network/egress block from Railway, not an auth bug. Also fixed the second latent bug: the emailed reset link was built from `APP_URL || FRONTEND_URL || "http://localhost:5173"`, so if those vars were unset the emailed link would point at `localhost`. The link is now derived from the request's own origin (`x-forwarded-proto` + `Host`), which works on any Railway domain without config; `APP_URL` / `FRONTEND_URL` still override. The mailer also logs which `host:port` it dials so connectivity issues are visible in prod logs. **Fix still requires a Railway-side change** (reachable SMTP relay — see below); code now surfaces it clearly | `server/src/controllers/auth.controller.ts`, `server/src/config/mailer.ts` |
| 31 | Grade Distribution (School-wide) — **hover tooltip read `0110`**. Root cause: `SUM()`/`AVG()` arrive from mysql2 as **strings** (`"110"`, DECIMAL columns), and the frontend reduced `0 + "110"` → `"0110"`. `serializeSubject()` in the grades controller now coerces every sum/avg with `Number()` (MAPEH aggregate too); both dashboards' `distData` reduces hardened with `Number(...)`; tooltip reworded to "Students" (verified live: `85-89 / Students : 2` — the old `0110` was corrupted; the real count was 2) | `server/src/controllers/grades.controller.ts`, `src/app/pages/registrar/RegistrarDashboard.tsx`, `src/app/pages/principal/PrincipalDashboard.tsx` |
| 32 | User Management (Admin) — **Status column showed a stale account status** (derived from `last_login` day counts; active accounts even showed "—"). Now shows **live presence**: new `users.last_seen_at` (migration `032_user_presence.sql`) is stamped on login, on a throttled heartbeat (`POST /api/presence/heartbeat`, auth-required, fire-and-forget from `AppContext` on login/mount + mousedown/keydown/scroll/touchstart/visibility-change, ≤1 beat/45 s), and cleared on logout. The admin list computes `online` (seen <2 min) / `idle` (2–15 min) / `offline`. Status column + filter now show **Online / Idle / Offline** badges with a "Last active…" tooltip, a pulsing **Live** badge, 30 s auto-refresh, and refresh-on-focus. Account status (active/idle/inactive) is untouched in the backend. Verified live end-to-end: login→online, heartbeat refreshes `last_seen_at`, logout→offline/`null`; page renders the signed-in admin **Online**, everyone else **Offline / Never seen** | `server/migrations/032_user_presence.sql`, `server/src/controllers/presence.controller.ts`, `server/src/routes/presence.routes.ts`, `server/src/controllers/users.controller.ts`, `server/src/controllers/auth.controller.ts`, `src/app/context/AppContext.tsx`, `src/app/services/presence.ts`, `src/app/pages/admin/UserManagement.tsx` |

### Pending — live testing

- [ ] SF forms (SF1/SF5) — visual check in the browser: text cells no longer reset to `0`, signatures show the account + principal names
- [ ] Activity Logs — desktop list scrolls within its card; row click still opens the viewer
- [ ] Backup **download** — new blob-based download needs a click in the browser (button works, endpoint verified); restore is verified end‑to‑end live
- [ ] Grade Upload **template** download — now same-tab navigation with `?token=`; needs a last click in the browser (verified to the download-commit point in a real Chrome e2e: 200 + correct filename)
- [ ] Basic reset link only exercised server‑side (SMTP configured) — take a look at the `/reset-password` page UI in the browser
- [ ] Subject delete for admins — delete a subject with grades/pending corrections → guidance message; an unused one → succeeds (backend verified live)

---

## 2. How to test the fixes

Prerequisites:
1. Restart the server once so the new migrations run (`029_room_occupied.sql`, `030_seed_legal_documents.sql`, `031_room_status_occupiable.sql`).
2. Logins: `admin`, `teacher01`, `registrar01` / `password123`.

| Fix | Where to test | Steps |
|-----|---------------|-------|
| LIS Excel | LIS panel (top bar, any role) | Open a learner profile that has sections → **Export LIS** → file downloads & opens (previously crashed) |
| Subject delete | Sections/Subjects (admin) | Delete a subject that has records → clear 409 message; delete an unused subject → succeeds |
| Backup download | Admin → Database Management | Create a backup → **Download** works (table row + mobile list); **Restore** → success and you stay logged in (verified live) |
| Room sync | Registrar → Scheduling + Admin → Rooms | Create a schedule with a room (Room dropdown) → room becomes **Occupied**; delete schedule → back to **Available**; same teacher booked in two rooms at overlapping times → **blocked 409**; set a room to **Maintenance**, schedule in it → stays **Maintenance** |
| User edit | Admin → User Management | Open a user → change **status** or role → Save → persists after reload |
| Profile edit | Any profile page → **Edit** | Edit Employee ID / Designation / Date Hired → Save → header + Employment card update |
| School settings | Admin → Settings | Clear Principal or Registrar → blocked; Active School Year is greyed out |
| Dashboard text | Admin dashboard + Principal dashboard | Stats read "Across all grades…", "Teachers with active accounts"; pie labels `Program: value` |
| Reset link | Login → Forgot Password? | Enter email → "Check your email" screen → open emailed link (or the amber dev link) → set new password → sign in with it |
| Legal docs | Login modals + Admin → Settings → Data Privacy | Full Terms/Privacy/Conditions show seeded on login; editor under "Legal Documents at Login" loads them; edit & save → login modals reflect the change |
| SF1/SF5 edit | Registrar → School Forms → SF1 / SF5 | Edit a name/LRN/remark cell → it keeps what you type (previously became `0`); signatures show the account name (Prepared by) and principal (Certified) |
| Subject management | Sections/Subjects (admin) | Click "Assigned Teacher" on a row → teacher(s) expand inline; the "Assign Teacher" action still opens the modal |
| Restore | Admin → Database Management | Create a backup → **Restore** → "Database restored successfully" and you stay logged in (verified live end‑to‑end) |
| Grade Summary PDF | Registrar → LIS Export → Grade Summary → **Official PDF** | Several landscape pages; Learner Name 200px on every page; each page shows ≤3 subjects with Q1–Q4 read from left to right (no vertical headers); Gen. Average / Promotion Status repeated per page; header row repeats on page breaks. Only subjects with grades in that school year appear (no more 20+ empty pages) |
| Grade Upload template | Teacher → Grade Management → Upload Grades | Select section + subject + school year, click **Download Excel Template** → `.xlsx` downloads from the server (no blank popup tab); matches LRN | Student Name | Grade |
| Profile save | Any profile page (Teacher / Registrar / Principal / Admin) → **Edit** | Change Full Name / Email / Phone / Date Hired → **Save Changes** → success toast, header + Employment card update, and the change persists after reload (previously every save returned a 500 because `getMe` sent `date_hired` back as ISO-8601, which MySQL's strict `DATE` column rejects). Validation: enter an invalid PH phone (e.g. `12345`) or a malformed email → inline red message + toast, Save blocked; valid values save fine |
| Enrollment period | Admin → Settings → School Configuration | Set **Enrollment Opens / Closes** → **Save Year & Enrollment Settings** → the two date fields keep their values after save and on page reload (previously the save response returned ISO-8601 timestamps, which `<input type="date">` rejects, so the fields blanked out to the `mm/dd/yy` placeholder right after saving) |
| SF10 seal | Registrar → School Forms → SF10 → **Print** | Look at the CERTIFICATION block: the "Affix School Seal here" circle sits in its own slot far right (no overlap with Date or the principal's signature line, and inside the box border). Resize the window (≥1024) — still no overlap |
| Academic year order | Admin → Academic Year Management | School Years list runs **latest first** (e.g. `2032, 2031, 2030 …` or `2026-2027, 2025-2026, …`), not oldest-first |
| Grade distribution | Registrar / Principal dashboard → **Grade Distribution (School-wide)** | Hover a grade-bucket bar → tooltip reads like `85-89 / Students : 2` (previously `0110 grades`) |
| User presence | Admin → User Management | Status column shows **Online** (green dot) for whoever is signed in right now — verify by opening the portal on a second account/browser; **Idle** after ~2 min without any mouse/keyboard/scroll; **Offline** otherwise (wait up to 15 min or watch it flip back to Offline after logout). Badge tooltip shows "Last active …"; the Status filter (Online/Idle/Offline) and pulsing **Live** badge work; the list refreshes itself every 30 s |